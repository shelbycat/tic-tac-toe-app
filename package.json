{
  "scripts": {
    "install": "cd ./games && npm ci",
    "start": "cd ./games && npm start",
    "build": "cd ./games && npm run build",
    "prettify": "prettier --write ."
  },
  "devDependencies": {
    "@ionic/cli": "^6.16.3",
    "native-run": "^1.0.0",
    "prettier": "^2.3.2"
  }
}
